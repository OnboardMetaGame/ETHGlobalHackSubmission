import "../styles/pool.scss";

const Pool = () => {
	return <div className="pool">Pool</div>;
};

export default Pool;

{
	/* <img
	className="to-home"
	onClick={() => setView("home")}
	src={BackBtn}
	alt="back"
/>; */
}
